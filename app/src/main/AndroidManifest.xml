<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.FoodMenu_v2"
        tools:targetApi="31">
        <activity
            android:name=".CheckSecurity"
            android:exported="false"
            android:label="@string/title_activity_check_security"
            android:theme="@style/Theme.FoodMenu_v2.NoActionBar" />
        <activity
            android:name=".Description"
            android:exported="false"
            android:label="Description" />
        <activity
            android:name=".ResumeOfOrder"
            android:exported="false"
            android:label="Finalizează comanda" />
        <activity
            android:name=".Start_Activity"
            android:exported="true"
            android:label="Food Menu" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>